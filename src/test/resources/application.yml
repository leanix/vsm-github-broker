leanix:
  vsm:
    base-url: http://localhost:${wiremock.server.port}
    event-broker:
      base-url: http://localhost:${wiremock.server.port}
    connector:
      api-token: api-token
      user-token: user-token
      config-name: git-on-prem-config
      github-token: github-token
      github-url: http://localhost:${wiremock.server.port}
    auth:
      access-token-uri: http://localhost:${wiremock.server.port}/services/mtm/v1
